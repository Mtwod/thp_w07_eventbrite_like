<%= render 'welcome' %>

<div class="container">  

  <% if @events.count > 0 %>

    <h3>Liste des évènements</h3>
    <div class="row">
      <%= render partial: 'event', collection: @events.reverse %>
    </div>

  <% else %>

      <h5 class="mb-1 text-center">Oh non ! 😱</h5>      
      <p class="mb-1 text-center">Il n'y a pas encore d'évènements 😢</p>
  
  <% end %>

</div> <!-- /container -->
